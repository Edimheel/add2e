// ADD2E — Marchand V2 reconstruit en unités courtes.
// Version : 2026-06-14-merchant-unit-v5-scene-buyers-price

import { findVendor, createVendor, getBuyer, isVendorActor, vendorKind, isStockItem, quantity, priceCopper, formatMoney, getMoney, buy, restockAll, setStock, assignItemToToken, alertBox, esc, lower, slug } from "./22a-vendor-core.mjs";

const VERSION = "2026-06-14-merchant-unit-v5-scene-buyers-price";
const arr = v => Array.isArray(v) ? v.flatMap(arr) : v == null || v === "" ? [] : typeof v === "string" ? v.split(/[,;|\n]+/g).map(x => x.trim()).filter(Boolean) : [v];
const key = v => slug(String(v ?? ""));
const uniq = v => [...new Set(v.filter(Boolean))];
function isShopActor(actor) { return isVendorActor(actor) || actor?.getFlag?.("add2e", "isArmorer") === true || /armurier/i.test(String(actor?.name ?? "")); }
function sceneActors() { const map = new Map(); for (const t of canvas?.tokens?.placeables ?? []) { const a = t?.actor; if (a?.id && !isShopActor(a)) map.set(a.id, a); } return [...map.values()].sort((a,b)=>String(a.name).localeCompare(String(b.name))); }
function buyerOptions(selected = "") { return sceneActors().map(a => `<option value="${esc(a.id)}" ${a.id === selected ? "selected" : ""}>${esc(a.name)}</option>`).join(""); }
function priceLabel(item) { const raw = String(item?.system?.prix ?? item?.system?.cout ?? "").trim(); return raw ? raw : formatMoney(priceCopper(item)); }
function linked(item) { const s = item?.system ?? {}, f = item?.flags?.add2e ?? {}; return uniq([...arr(s.sorts_associes), ...arr(s.sortsAssocies), ...arr(s.spells), ...arr(s.spellNames), ...arr(f.sorts_associes), ...arr(f.sortsAssocies), ...arr(f.spells), ...arr(f.spellNames)].map(x => String(x).trim()).filter(Boolean)); }
function spellKeys(spell) { const s = spell?.system ?? {}, f = spell?.flags?.add2e ?? {}; return uniq([spell?.name, s.nom, s.slug, f.slug, f.importKey].map(key)); }
function memorized(spell) { try { const n = Number(globalThis.add2eGetTotalMemorizedCount?.(spell)); if (Number.isFinite(n) && n > 0) return Math.floor(n); } catch (_e) {} const raw = spell?.getFlag?.("add2e", "memorizedByList") ?? spell?.flags?.add2e?.memorizedByList ?? {}; if (raw && typeof raw === "object" && !Array.isArray(raw)) return Object.values(raw).reduce((s,v)=>s+(Number(v)||0),0); return Number(spell?.getFlag?.("add2e", "memorizedCount") ?? spell?.flags?.add2e?.memorizedCount ?? 0) || 0; }
function spells(actor) { return Array.from(actor?.items ?? []).filter(i => String(i?.type ?? "").toLowerCase() === "sort" && !i.system?.isPower && !i.system?.isObjectPower).sort((a,b)=>String(a.name).localeCompare(String(b.name))); }
function usage(actor, item) { if (vendorKind(item) !== "Composant") return { known:0, prep:0, names:[] }; const aliases = linked(item).map(key); if (!aliases.length) return { known:0, prep:0, names:[] }; let known = 0, prep = 0, names = []; for (const sp of spells(actor)) if (spellKeys(sp).some(k => aliases.includes(k))) { known += 1; const m = memorized(sp); prep += m; names.push(m > 1 ? `${sp.name} ×${m}` : sp.name); } return { known, prep, names: uniq(names) }; }

class Add2eMerchantApp extends foundry.applications.api.ApplicationV2 {
  static DEFAULT_OPTIONS = { id: "add2e-merchant-{id}", classes: ["add2e", "add2e-merchant-app"], tag: "section", window: { title: "Marchand ADD2E", resizable: true }, position: { width: 1080, height: 680 } };
  constructor({ vendor, buyer } = {}, options = {}) { super(options); this.vendor = vendor; this.buyer = buyer ?? getBuyer(); this.tab = "all"; this.search = ""; }
  get title() { return `${this.vendor?.name ?? "Marchand"}${this.buyer ? ` — ${this.buyer.name}` : ""}`; }
  stock() { return Array.from(this.vendor?.items ?? []).filter(isStockItem).sort((a,b)=>String(a.name).localeCompare(String(b.name))); }
  async _prepareContext() { return { vendor: this.vendor, buyer: this.buyer, items: this.stock(), isGM: game.user?.isGM === true, tab: this.tab, search: this.search }; }
  async _renderHTML(ctx) { let prepCount=0, knownCount=0; const rows=ctx.items.map(i=>{const kind=vendorKind(i),u=usage(ctx.buyer,i),tabs=[kind==="Composant"?"components":kind==="Projectile"?"projectiles":"equipment"]; if(u.prep>0){tabs.push("prepared");prepCount++;} if(u.known>0){tabs.push("known");knownCount++;} const txt=lower(`${i.name} ${kind} ${u.names.join(" ")}`); const visible=(ctx.tab==="all"||tabs.includes(ctx.tab))&&(!ctx.search||txt.includes(lower(ctx.search))); const gm=ctx.isGM?`<td><input class="s" type="number" min="0" value="${quantity(i)}" style="width:54px"><button data-action="stock">Stock</button><button data-action="assign" ${!ctx.buyer||quantity(i)<=0?"disabled":""}>Donner</button></td>`:""; return `<tr data-id="${esc(i.id)}" style="${visible?"":"display:none"}"><td>${esc(i.name)}</td><td>${esc(kind)}</td><td>${u.prep?`Mém. ${u.prep}`:""} ${u.known?`Conn. ${u.known}`:""}</td><td>${esc(u.names.length?u.names.join(", "):"—")}</td><td>${esc(priceLabel(i))}</td><td>${quantity(i)}</td><td><input class="q" type="number" min="1" value="1" style="width:54px"></td><td><button data-action="buy" ${!ctx.buyer||quantity(i)<=0?"disabled":""}>Acheter</button></td>${gm}</tr>`;}).join(""); const nav=[["all","Tous"],["prepared",`Composants sorts mémorisés (${prepCount})`],["known",`Composants sorts connus (${knownCount})`],["components","Composants"],["projectiles","Projectiles"],["equipment","Équipement"]].map(([id,lab])=>`<button data-tab="${id}" ${ctx.tab===id?"style='outline:2px solid #fff'":""}>${lab}</button>`).join(""); const buyer=ctx.isGM?`<select class="buyer">${buyerOptions(ctx.buyer?.id)}</select>`:`<b>${esc(ctx.buyer?.name??"aucun")}</b>`; const div=document.createElement("section"); div.style.cssText="height:100%;overflow:auto;background:#f1d99c;color:#231706;padding:10px"; div.innerHTML=`<h2>${esc(ctx.vendor?.name??"Marchand")}</h2><p>Acheteur : ${buyer} — ${ctx.buyer?esc(formatMoney(getMoney(ctx.buyer))):""}</p><div>${nav}${ctx.isGM?`<button data-action="restock">Restock global</button>`:""}</div><input class="search" value="${esc(ctx.search)}" placeholder="Recherche" style="width:100%"><table style="width:100%;background:#fff7df"><thead><tr><th>Article</th><th>Type</th><th>Usage</th><th>Sorts</th><th>Prix</th><th>Stock</th><th>Qté</th><th></th>${ctx.isGM?"<th>MJ</th>":""}</tr></thead><tbody>${rows}</tbody></table>`; return div; }
  _replaceHTML(result, content) { content.replaceChildren(result); }
  async _onRender(c,o) { await super._onRender?.(c,o); const r=this.element; r.querySelector(".buyer")?.addEventListener("change", e=>{this.buyer=game.actors.get(e.currentTarget.value)??this.buyer;this.render({force:true});}); r.querySelector(".search")?.addEventListener("input", e=>{this.search=e.currentTarget.value??"";this.render({force:true});}); r.querySelectorAll("button[data-tab]").forEach(b=>b.addEventListener("click", e=>{this.tab=e.currentTarget.dataset.tab;this.render({force:true});})); r.querySelectorAll("button[data-action]").forEach(b=>b.addEventListener("click", e=>this.click(e))); }
  async click(e) { const a=e.currentTarget.dataset.action; if(a==="restock"){await restockAll(this.vendor);return this.render({force:true});} const row=e.currentTarget.closest("tr"); const item=this.vendor.items.get(row?.dataset.id); if(a==="stock"){await setStock(item,row.querySelector(".s")?.value);return this.render({force:true});} if(a==="assign")return this.assign(item); const ok=await buy({vendor:this.vendor,buyer:this.buyer,item,quantity:row.querySelector(".q")?.value}); if(ok&&game.user?.isGM)this.render({force:true}); }
  async assign(item) { if(!this.buyer)return alertBox("Aucun acteur","Choisis d’abord un acteur acheteur présent sur la scène."); const r=await assignItemToToken({vendor:this.vendor,item,token:{actor:this.buyer,name:this.buyer.name},quantity:1}); r.ok?ui.notifications.info(r.message):ui.notifications.warn(r.message); this.render({force:true}); }
}

const registry = () => globalThis.__ADD2E_MERCHANT_UNIT_APPS ??= new Map();
function openLock(actor) { const id = `${game.user?.id}:${actor?.id}`; const now = Date.now(); const m = globalThis.__ADD2E_MERCHANT_OPEN_LOCK ??= {}; if (m[id] && now - m[id] < 750) return false; m[id] = now; return true; }
async function openFromToken(token) { if (!isVendorActor(token?.actor) || !openLock(token.actor)) return false; await openVendor({ vendor: token.actor, buyer: getBuyer() }); return true; }
export async function openVendor({ vendor = null, buyer = null } = {}) { vendor = vendor ?? findVendor(); if (!vendor && game.user?.isGM) vendor = await createVendor(); if (!vendor) return alertBox("Marchand introuvable", "Le marchand doit être créé côté MJ."); buyer = buyer ?? getBuyer(); if (!buyer && !game.user?.isGM) return alertBox("Aucun acheteur", "Aucun personnage assigné ou sélectionné."); const id = `${game.user?.id}:${vendor.id}`; const old = registry().get(id); if (old?.rendered) { old.vendor=vendor; old.buyer=buyer; old.render({force:true}); old.bringToFront?.(); return old; } const app = new Add2eMerchantApp({ vendor, buyer }); registry().set(id, app); app.render({ force:true }); return app; }
export function bindAllVendorTokens() { for (const token of canvas?.tokens?.placeables ?? []) { if (!isVendorActor(token?.actor) || token.__add2eMerchantTapV4) continue; token.__add2eMerchantTapV4=true; try { token.cursor="pointer"; token.eventMode="static"; token.interactive=true; token.on?.("pointertap", ev=>{ev?.stopPropagation?.(); openFromToken(token);}); token.on?.("pointerup", ev=>{ev?.stopPropagation?.(); openFromToken(token);}); } catch (_e) {} } }
export function patchVendorTokenClick() { if (globalThis.__ADD2E_MERCHANT_UNIT_CLICK_V4) return; globalThis.__ADD2E_MERCHANT_UNIT_CLICK_V4=true; const C=foundry?.canvas?.placeables?.Token??CONFIG?.Token?.objectClass??globalThis.Token; const p=C?.prototype; if(p&&typeof p._onClickLeft==="function"){const old=p._onClickLeft;p._onClickLeft=function(event){const result=old.call(this,event);if(isVendorActor(this.actor))setTimeout(()=>openFromToken(this),0);return result;};} Hooks.on("canvasReady", bindAllVendorTokens); Hooks.on("createToken",()=>setTimeout(bindAllVendorTokens,100)); Hooks.on("updateToken",()=>setTimeout(bindAllVendorTokens,100)); setTimeout(bindAllVendorTokens,500); }
export function registerVendorDirectoryButton() { Hooks.on("renderActorDirectory", (_app, html) => { if (!game.user?.isGM) return; const root=html?.jquery?html[0]:html; if(!root?.querySelector||root.querySelector(".add2e-open-default-vendor"))return; const button=document.createElement("button"); button.type="button"; button.className="add2e-open-default-vendor"; button.textContent="Marchand"; button.addEventListener("click",()=>openVendor()); root.querySelector(".directory-footer")?.prepend(button); }); }
export function registerUiGlobals() { game.add2e=game.add2e??{}; game.add2e.openVendor=openVendor; game.add2e.vendorAppVersion=VERSION; }