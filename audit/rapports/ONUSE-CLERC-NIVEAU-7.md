# Rapport onUse — Clerc niveau 7
## Diagnostic
- Référence lue : `audit/reference/manuel-joueurs-clerc-niveau-7.json`.
- Aucun JSON Foundry n'a été modifié.
- Les scripts créés dans ce lot sont des aides MJ DialogV2 lorsque l'automatisation complète serait incertaine.

## Scripts créés
- Aucun : tous les slugs attendus existaient déjà dans `scripts/sorts/`.

## Scripts modifiés
- Aucun script existant n'a été modifié : les onUse présents ont été préservés pour éviter une régression fonctionnelle.

## Scripts laissés inchangés
- `Contrôle du climat` : script existant ou chemin historique conservé.
- `Marche des vents` : script existant ou chemin historique conservé.
- `Parole sacrée/maudite` : script existant ou chemin historique conservé.
- `Régénération` : script existant ou chemin historique conservé.
- `Restauration` : script existant ou chemin historique conservé.
- `Résurrection` : script existant ou chemin historique conservé.
- `Seuil` : script existant ou chemin historique conservé.
- `Sort astral` : script existant ou chemin historique conservé.
- `Symbole` : script existant ou chemin historique conservé.
- `Tremblement de terre` : script existant ou chemin historique conservé.

## Sorts automatisés
- Aucun nouveau sort complexe n'a reçu d'automatisation destructive dans ce lot.
- Les scripts existants déjà fonctionnels sont conservés tels quels.

## Sorts en aide MJ
- Aucun nouveau script d'aide MJ créé pour ce niveau.

## Limites connues
- Test runtime Foundry non exécuté dans l'environnement Codex.
- Les scripts historiques qui contiennent déjà dialogue legacy V1 sont une dette technique préexistante et ne sont pas réécrits dans ce lot.
- Les effets d'invocation, de déplacement, de zone persistante ou d'interprétation restent à arbitrer par le MJ si le script existant ne les automatise pas de façon fiable.

## Fichiers touchés
- `audit/rapports/ONUSE-CLERC-NIVEAU-7.md`
